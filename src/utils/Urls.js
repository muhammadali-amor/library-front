export const CDN_DOWNLOAD = (photoId) => {
    return `https://ucarecdn.com/${photoId}/-/preview/1000x625/`
}

export const CDN_DOWNLOAD_VIDEO = (videoId) => {
    return `https://ucarecdn.com/${videoId}/`
}